#!/bin/bash
#Don't use trailing slashes for path configs
BACKUPPATH="/backups/Minecraft"
BACKUPNAME="Minecraft-Server"

#These paths must be within the server's directory, and are relative to it
BUILDPATH="build"
JARPATH="jars"

MINRAM="2G"
MAXRAM="16G"

VERSION="1.21.10"
